{
  "name": "apollo-prometheus-plugin",
  "version": "1.0.0",
  "description": "A GraphQL plugin to collect and report resolver metrics with Prometheus",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": [
    "graphql",
    "prometheus",
    "metrics",
    "plugin",
    "apollo-server"
  ],
  "author": "ijs",
  "dependencies": {
    "prom-client": "^15.1.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}